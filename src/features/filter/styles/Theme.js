import { createTheme } from '@mui/system';
const theme = createTheme({
    palette: {
      background: {
        primary: "#C89D66",
        secondary: "#333642",
      },
    },
  });

export default theme 